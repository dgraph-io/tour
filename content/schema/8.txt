{
  delete {
    # Delete a single triple (S P O).
    # You must know the exact value to delete.
    <0x1c3eb> <name> "Steven" .
    <0x1c3eb> <age> "38" .

    # Delete an edge between two nodes (S P O).
    <0x1c3eb> <friend> <0x1c2a1> .

    # Delete all values for a predicate (S P *).
    # Removes all friends from this node.
    <0x1c3eb> <friend> * .

    # Delete all predicates for a node (S * *).
    # IMPORTANT: Only deletes predicates defined in the node's
    # dgraph.type. Ensure the node has a type set and that the
    # type is defined in your schema.
    <0x1c3eb> * * .
  }
}
