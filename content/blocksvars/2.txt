{
  # Define a variable 'SF' containing all Steven Spielberg films
  var(func: allofterms(name@en, "Steven Spielberg")) {
    SF as director.film
  }

  # Use the variable to query those films
  spielberg_films(func: uid(SF)) {
    name@en
    initial_release_date
  }
}
